
  # SistemaVentas

  This is a code bundle for SistemaVentas. The original project is available at https://www.figma.com/design/zNSAALusUEY1LWyppU1UWC/SistemaVentas.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  